#!/bin/ksh
AWK=awk
log=$1
ERRLIST=""
. ./functions || { echo "current dir is not the test dir" ; exit 1; }

check_scf_energy DFT  -525.926546747110 1e-7

if [ "$ERRLIST" = "" ]; then
  echo TEST ENDED PROPERLY
  exit 0
else
  echo "Failed tests: $ERRLIST"
  echo THERE IS A PROBLEM
  exit 1
fi
